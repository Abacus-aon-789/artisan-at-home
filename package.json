{
  "name": "artisan-at-home",
  "version": "1.0.0",
  "private": true,
  "description": "Artisan at Home website — static site with a Stripe checkout function on Vercel.",
  "dependencies": {
    "stripe": "^16.12.0"
  }
}
